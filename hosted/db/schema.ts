import {sqliteTable,text,integer,index} from "drizzle-orm/sqlite-core";
export const restaurants=sqliteTable("restaurants",{id:text("id").primaryKey(),owner:text("owner").notNull().unique(),name:text("name").notNull(),city:text("city").notNull(),created:text("created").notNull()});
export const records=sqliteTable("records",{id:text("id").primaryKey(),restaurantId:text("restaurant_id").notNull().references(()=>restaurants.id),kind:text("kind").notNull(),data:text("data").notNull(),created:text("created").notNull()},t=>[index("records_restaurant_kind").on(t.restaurantId,t.kind)]);


export const stockLots=sqliteTable("stock_lots",{id:text("id").primaryKey(),restaurantId:text("restaurant_id").notNull().references(()=>restaurants.id),ingredientId:text("ingredient_id").notNull(),name:text("name").notNull(),unit:text("unit").notNull(),initial:integer("initial").notNull(),remaining:integer("remaining").notNull(),totalCost:integer("total_cost").notNull(),supplier:text("supplier").notNull(),expires:text("expires"),location:text("location").notNull(),created:text("created").notNull()},t=>[index("stock_restaurant").on(t.restaurantId)]);
export const stockMoves=sqliteTable("stock_moves",{id:text("id").primaryKey(),restaurantId:text("restaurant_id").notNull().references(()=>restaurants.id),lotId:text("lot_id").notNull().references(()=>stockLots.id),quantity:integer("quantity").notNull(),applied:integer("applied").notNull().default(0),cost:integer("cost").notNull(),reason:text("reason").notNull(),created:text("created").notNull()},t=>[index("moves_restaurant").on(t.restaurantId)]);
